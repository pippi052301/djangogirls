from django.test import TestCase

# Create your tests here.
class ai_enginePageTest(TestCase):
    def test_ai_engine_page_opens(self):
        response = self.client.get("/ai/")

        self.assertEqual(response.status_code, 200)
        self.assertContains(response, "AI Engine Home")

#TestData
QUESTION = """
二次方程式における判別式とは何か。
また、判別式の値と実数解の個数との関係を説明しなさい。
"""

REFERENCE_ANSWER = """
判別式とは、二次方程式 ax² + bx + c = 0 において、
実数解の個数を判別するために用いられる
D = b² - 4ac のことである。

D > 0 のとき異なる2つの実数解をもち、
D = 0 のとき1つの実数解（重解）をもち、
D < 0 のとき実数解をもたない。
"""

td_09 = """
判別式とは、二次方程式 ax²+bx+c=0 の実数解の個数を判別するために用いられる D=b²-4ac のことである。
D>0なら異なる2つの実数解、D=0なら1つの実数解（重解）、D<0なら実数解をもたない。 
"""

td_10 = """
二次方程式にはDというのがあってb²-4acで計算する。これを見ると答えが何個あるかわかる。
プラスなら2個で、0なら同じものが1個、マイナスなら実数の答えはない。
"""

td_11 = """
判別式は D=b²-4ac であり、二次方程式の実数解の個数を判断するために使われる。
D>0の場合、異なる2つの実数解をもつ。 
"""

td_12 = """
D<0なら実数解はない。また、二次方程式では解の個数を調べることができる。
D=b²-4acを判別式という。異なる2つの実数解になるのはD>0の場合である。
判別式が0なら重解となり、実数解は1つである。 
"""

td_13 = """
二次方程式では、b²-4acという計算をして、その数字を使って答えがいくつあるか調べられる。
その数字が0より大きければ答えは2つ、0なら同じ答えが1つ、0より小さければ普通の数字の範囲では答えがない。 
"""

td_14 = """
判別式は D=b²+4ac で求められ、二次方程式の解そのものを計算するための公式である。
D>0なら実数解はなく、D=0なら異なる2つの実数解をもち、D<0なら1つの実数解をもつ。
"""